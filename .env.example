# Supabase QA project: Project Settings → API (or the "Connect" button).
# The publishable key is safe in the browser; row level security protects the data.
NEXT_PUBLIC_SUPABASE_URL=your-qa-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-qa-publishable-key

# Ref of the Production project (the id in its URL: https://<ref>.supabase.co).
# Not a secret. Used at release time to relink the CLI before applying
# migrations to Production.
SUPABASE_PRODUCTION_PROJECT_REF=your-production-project-ref

# Server-only: read in services/news.ts, never sent to the browser.
NEWS_API_URL=https://hn.algolia.com/api/v1

# Public: the NEXT_PUBLIC_ prefix inlines it into the client bundle.
# Set to "false" to hide the Stats page and nav link.
NEXT_PUBLIC_ENABLE_STATS=true

# Server-only secret: read in app/actions/like-session.ts, never sent to the
# browser. The URL itself is the credential — anyone with it can post to it.
# Get a free one at webhook.site (no signup, expires after 7 days — create it
# the day you need it). Differs per environment (local/preview/production).
FEEDBACK_WEBHOOK_URL=https://webhook.site/your-unique-url
